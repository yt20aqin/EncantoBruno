name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ secrets.ANDROID_KEYALIAS_PASS }} | base64 -d > .env.production
        shell: bash
        env:
         SSH_KEY: ${{secrets.ANDROID_KEYALIAS_PASS}}
